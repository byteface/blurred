[package]
name = "blurred"
version = "0.2.0"
edition = "2021"

[dependencies]
directories = "5"
fltk = { version = "1", features = ["fltk-bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
strip = true
